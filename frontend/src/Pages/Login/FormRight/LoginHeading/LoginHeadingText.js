const LoginHeadingText = () => {
  return (
    <h1
      className={` w-full text-heading2 font-semibold  text-center tracking-wide mb-3 `}
    >
      Log into My Diary
    </h1>
  );
};

export default LoginHeadingText;
